/**
 * Marker interface for domain services.
 * A domain service encapsulates domain logic that doesn't belong in a single entity or VO.
 * Implementations are stateless and infrastructure-free.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface DomainService {}
