import { StatusPage } from "../../components/StatusPage";

export default function UserDetailPage() {
  return (
    <StatusPage
      title="User profile"
      description="The selected user profile will be loaded from the IAM API."
    />
  );
}
